import { Text, View,route } from 'react-native';
export const Expenses = () => {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center',flexDirection:"column" }}>
        <Text style={{fontSize:30,color:'purple'}}>HELLO</Text>
        
      </View>
    );
  }